{
  "mcpServers": {
    "shadcn": {
      "command": "npx",
      "args": [
        "shadcn@latest",
        "mcp"
      ]
    },
    "colorsandfonts": {
      "command": "npx",
      "args": [
        "-y",
        "@colorsandfonts/mcp"
      ]
    },
    "PayloadLive": {
      "type": "http",
      "url": "https://ryulawyer.com/api/mcp",
      "headers": {
        "Authorization": "Bearer ${PAYLOAD_MCP_API_KEY}"
      }
    }
  }
}