import { PagePlaceholder } from '@/app/(frontend)/_components/PagePlaceholder'

export default function NonCriminalHubPage() {
  return (
    <PagePlaceholder
      route="/services/non-criminal"
      title="민사·가사·행정"
      description="비형사 2 leaf hub (민사·가사 / 행정). 형사와 연동되는 사건(음주운전 면허취소·공무원 징계 등)에서 USP 연결."
    />
  )
}
